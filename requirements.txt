# Core Dependencies
python-dateutil>=2.8.2
pytz>=2023.3

# Data Processing
numpy>=1.24.0,<2.0.0  # Pin to 1.x - vectorbt and numba not yet compatible with 2.x
pandas>=2.0.0
scipy>=1.10.0

# Backtesting & Financial Analysis
# vectorbt is OPTIONAL - commented out due to incompatibility with NumPy 1.26 on Python 3.10
# The system gracefully handles missing vectorbt (falls back to standard BacktestEngine)
# vectorbt>=0.26.0  # DISABLED: requires numba<0.57 which needs numpy<1.24
quantstats>=0.0.62
empyrical-reloaded>=0.5.5  # Python 3.13 compatible fork of empyrical

# Data Sources
yfinance>=0.2.28
ccxt>=4.0.0
requests>=2.31.0

# Visualization
plotly>=5.17.0
matplotlib>=3.7.0
seaborn>=0.12.0

# Frontend
streamlit>=1.28.0
streamlit-aggrid>=0.3.4

# Configuration
pyyaml>=6.0.1
toml>=0.10.2
python-dotenv>=1.0.0

# Type Checking & Code Quality
mypy>=1.5.0
types-PyYAML>=6.0.12
types-toml>=0.10.8
types-requests>=2.31.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.21.0
pytest-mock>=3.11.0

# Utilities
tqdm>=4.66.0
loguru>=0.7.0
click>=8.1.7

# Development
black>=23.7.0
ruff>=0.0.290
pre-commit>=3.4.0
